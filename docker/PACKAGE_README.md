# pii-zh-qwen inference package

This minimal file supplies wheel metadata inside the allowlisted inference-image build context.
The image contains project code only: no model weights, credentials, private data, host paths or
runtime service configuration are embedded. Deployment and evidence boundaries remain in the
repository documentation outside the image.
